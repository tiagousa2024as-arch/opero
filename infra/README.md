# infra (AWS CDK, Phase 2+)

TypeScript CDK stacks — VPC, RDS PostgreSQL, S3, SES, Secrets Manager, App Runner — per PART F, Phase 2 of the blueprint. Not built out yet: it requires a real AWS account with billing configured (see PART G) before `cdk deploy` can run against anything.
